import { Callout } from "nextra-theme-docs";

# Getting Started

<Callout type="info">

**`plaiceholder` packages are [ESM only](https://gist.github.com/sindresorhus/a39789f98801d908bbc7ff3ecc99d99c).**

</Callout>

<Callout type="info">

Plaiceholder is a server-side library. It will **not** work in the browser.

</Callout>

## Prerequisites

Under-the-hood, Plaiceholder uses the wonderful and powerful [`sharp`](https://sharp.pixelplumbing.com/) library to transform images.

Some frameworks or libraries include `sharp` by default, so double-check before you install.

```sh
npm install sharp
```

## Installation

```sh
npm install plaiceholder
```
