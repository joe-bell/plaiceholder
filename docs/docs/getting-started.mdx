---
sidebar_position: 2
---

import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

# Getting Started

:::note

Plaiceholder is a [Node.js](https://nodejs.org/en/) library.
It's designed **only** to work in a Node.js environment, **not** the browser.

:::

## Prerequisites

Under-the-hood, Plaiceholder uses the wonderful and powerful [`sharp`](https://sharp.pixelplumbing.com/) library to transform images.

Some frameworks or libraries include `sharp` by default, so double-check before you install.

<Tabs
  groupId="package-manager"
  defaultValue="npm"
  values={[
    { label: "npm", value: "npm" },
    { label: "Yarn", value: "yarn" },
  ]}
>
<TabItem value="npm">

```sh
npm i sharp
```

</TabItem>
<TabItem value="yarn">

```sh
yarn add sharp
```

</TabItem>
</Tabs>

## Installation

<Tabs
  groupId="package-manager"
  defaultValue="npm"
  values={[
    { label: "npm", value: "npm" },
    { label: "Yarn", value: "yarn" },
  ]}
>
<TabItem value="npm">

```sh
npm i plaiceholder
```

</TabItem>
<TabItem value="yarn">

```sh
yarn add plaiceholder
```

</TabItem>
</Tabs>
